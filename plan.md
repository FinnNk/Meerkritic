# Frozen semantic plan

Verified diary: 020e838ed47725f801816b058090a45e6a3d72b1. Base: 2a7b118042c087dc0ed58f7068bee7f1433c64c0. Freeze follows isolated diary verification (43 tests, all canonical gates). No live provider calls or hosted CI are claimed. Scope: route safely, account honestly, retain immutable provenance, inspect/export the records.

1. **Eligible selection** — inventory/policy/task contracts, strict validation, precedence, privacy/capability/context/health/budget filters, explained refusal, synthetic configuration and contract tests/docs. No storage or provider execution. Requires only the baseline locked stack; explicitly declare existing Pydantic dependency.
2. **Usage accounting** — token subsets, unknown telemetry, distinct failure outcomes, historical price effectiveness, API cost calculation and terse display. Tests/docs/glossary accompany the API. Requires selected decisions from 1; no persistence yet.
3. **Durable provenance** — journal/application contract, immutable version identities, transaction and retry behaviour, migration preserving dataset history and referential restrictions, shared SQLite owner, ADR-0005 and storage tests. Requires 1–2. No operator CLI or export claim at this checkpoint.
4. **Operator inspection and analytics** — preview/record/inspect commands, external-path enforcement, immutable Parquet snapshot including version payloads, CLI/export tests, operational guide and current progress/backlog. Requires 1–3; synthetic examples never claim real availability or inference.

Alternative: the original three-unit sketch combined journal and operator/export surfaces. Separate review is meaningful at the RoutingJournal API: transaction correctness is established independently of CLI parsing and file publication. Four complete checkpoints avoid making the storage review also inspect shell-facing input/output or Parquet failures. A single unit would unnecessarily mix those claims. Splitting schemas, tests or docs into standalone commits would leave incomplete guarantees. No new production abstraction was introduced solely to create commits.

Implementation file boundaries need not match commits. The journal's export method and related imports, and the relevant test methods, are introduced at checkpoint 4; frozen final bytes are restored exactly. One price test gains its export assertions there. No continuing test obligation is retired; the baseline architecture test follows the moved SQLite owner in checkpoint 3. The three architecture negative controls remain unchanged.

Verification: clean separate worktree and uv environment for every checkpoint; canonical quality command, own source location and package/lock identity, clean state and exact commit/tree. Final snapshot equivalence and aggregate inspection remain separate obligations. A challenge that shows storage cannot be assessed without the operator implementation would invalidate the chosen split; current contracts do not exhibit that dependency.
