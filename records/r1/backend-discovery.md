# Persistence discovery during implementation

The first nine backend tests failed six cases at the existing annotation_valid_result SQLite trigger: it allowed successful jobs only. Application eligibility alone is insufficient. Retain backend-tests.log. Add a forward migration replacing that trigger with the authorised terminal-result policy and same-job artefact checks. Existing rows and immutable update/delete triggers remain unchanged. Semantic/raw-body eligibility stays in the application owner; SQLite enforces terminal state, allowed failed actions and exact artefact identity without duplicating large bodies. Direct-store negative tests exercise this second boundary. This remains within the approved contract; no quality or architecture checks are weakened. The two long-line lint failures are corrected without ignores.

The second run exposed two test-setup mistakes: strict nested interpretation parsing needs the JSON validation entry point, and the browser fixture must use a trusted local host. Both original failing logs are retained. After correcting the URL in the actual formatted fixture, all ten backend tests pass (backend-tests-04.log). No application assertion was weakened.

Full P2 and diary verification exposed test_existing_results_and_new_edits_gain_verified_metadata: a pre-catalogue successful body intentionally lacks job_id. New eligibility had rejected that supported legacy shape. Restore absent identity compatibility only for successful bodies; explicit conflicting identities and failed bodies remain strict. Existing regression retained unchanged. No semantic series or round snapshot has yet been created; preserve the failed checkpoint logs, repair diary-first and refreeze.

The corrected diary full run passed application assertions but hit the recurring Windows temporary-Git cleanup lock. Bounded isolated reproduction failed both once immediately and once after six passes; own/external handle inventories did not identify a remaining holder. Forty identical isolated runs passed in the normal Windows temporary directory. Subsequent full verification uses that process-local TEMP/TMP setting, recorded in verify_system_temp.py and each check record. No global settings, test assertions or cleanup policy changed. This is an operational mitigation, not a proven root-cause attribution. All original failures remain.
