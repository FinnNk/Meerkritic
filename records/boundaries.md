# Batch B semantic boundary plan

Pair vs2-rules/r1. Frozen candidate 7d782647b031d8599fafc128e100e7ad40d5daf2,
subject to its clean-checkout checks. Diary base 125fd8c055b9c110efa1c4dd31dcc661358b32bc;
semantic base e81c2e867e714f2fed72d235eb44ba035b4bf302. The owner rebased/merged PR11;
the two base tracked trees are exactly equal. Do not merge archival ancestry.

What matters: immutable version/source identity, evidence preserved across changes,
atomic fenced research decisions, bounded local runtime work, distinct invalid and
insufficient results, inspectable interrupted publication and true provenance.

P1: activate Batch B against the verified merged prerequisite, including ADR10 status.
No software claim; baseline 132 tests and architecture checks in its own environment.

P2: establish complete immutable rule registry and immediate human review, including
source resolution, model-origin trace contract, weak evidence inheritance, revision
bounds, CAS/idempotency, UI, composition, ADR11 and operation documentation. Creation
is available through RuleService.propose, used by synthetic contract tests; no model
workflow is promised yet. It depends on the existing cluster/selection contracts.
Tests must prove stale/concurrent/event failure and preservation, not just happy CRUD.

P3: apply that registry through bounded routed real MAF synthesis and corpus jobs,
including versioned policy, queue/worker/CLI/UI integration, old-manifest compatibility,
running telemetry, failure/insufficiency/raw trace, interruption and live observations.
No implicit replay or quality/adoption claim. Canonical checks plus exact final tree.

Alternative considered: one implementation proposition for all registry and synthesis.
That hides a independently challengeable evidence/version contract. Splitting by
domain/storage/web layers instead would borrow correctness from later commits. The
chosen sequence lets the second proposition rely on a tested publication contract.
Challenge: if P2 needs synthesis-private knowledge to verify provenance, combine the
shared obligation rather than inventing a facade. Its explicit trace fields and
negative model-origin test suffice without concrete MAF or provider types.

All checkpoints own source, tests, lock and installed package. Final tracked-tree
equivalence includes all documentation/tests. Author review is self-review, not an
independent review. Publication/host qualification/owner acceptance stay separate.
