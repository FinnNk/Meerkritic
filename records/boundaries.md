# Final VS2 batch: semantic propositions

Frozen candidate f6de3b9bb5c119e516ccb270a3b1b95a34a81f6d, subject to clean-checkout
verification. Diary base dc6ba77119b2cf148cf014c54a7748953bd9735d; semantic base
ff9af2e68d989590ebe030002e2e3789c35efef0. The owner merged PR12 while C was built;
the base tracked trees are equal. Target main now; do not merge diary ancestry.

Material invariant map: saved intent is not applied; all operational decisions and
events apply atomically; exact-version source/claims remain immutable; reopening is
explicit; submitted context cannot consume later discussion; responses are advisory;
unknown external completion never triggers replay; stale architecture is visible.

P1 guidance/activation: refine existing contract checks, freeze C and reconcile the
owner-integrated prerequisite/ADR11. No software feature or empirical adoption claim.
P2 existing-code comments: backfill only pre-existing discovery/rule HTTP contracts.
P3 complete staged interaction: registry-body verification, canonical transaction
composition, persistent drafts/lifecycle/discussion, UI, tests and ADR12/operations.
No model send promised at this checkpoint. Shared obligations stay in one proposition.
P4 complete guidance application: immutable exact-version context, replay-safe queue,
shared worker fairness, routed real MAF advice, response validation/unknown completion,
UI and operational evidence. P3 contracts are prerequisites, not implementation leaks.
P5 architecture and milestone closure: shared typed generator, source freshness,
before/delta/after projection, acceptance tests, all-guidance findings, slice review
and explicit future-slice revision. Honest human/EDR/owner gates remain outstanding.

Compared alternatives: one large interaction proposition obscures the important
boundary between human decision application and non-applying agent advice. Separate
PRs per storage/runtime/UI would leave incomplete promises and excessive review
overhead. Five semantic propositions inside the final substantial batch preserve
those contracts; guidance and existing-code backfill remain separate as requested.
If a checkpoint requires later code to satisfy its guarantee, return to the diary
or combine the shared obligation rather than accepting a knowingly broken boundary.

Each checkpoint supplies its own clean source/tests/lock/installed package on
Windows/Python3.12. Final diary and semantic trees must be exactly equal. Review is
author self-review; publication, hosted qualification, acceptance and integration
remain distinct. No significant empirical choice is made from incidental tests.
