# Final browser observation

Final source fc30a333e950270356d99b29ab06cf84ca91154c, IAB tab 4, loopback port 8001.
From public record 2, Normalise returned queued job 563c3c68-f783-46ca-a0ad-b53bc41ee210.
The separately running final-checkpoint worker completed it. The automatically refreshed
browser showed succeeded, Proposed issue, actionable/generalisable fields, categories,
scope, invariant, two source spans, exclusions and collapsed provenance/identity panels.
Telemetry: qwen3-4b-local · 212 in / 328 out · local · 4.5s.

The source quotes matched exactly; the interpretation speculated about other potential
bugs. The page explicitly states that matching evidence does not establish correctness.
This is functional evidence, not a model-quality acceptance or empirical comparison.
An earlier visual check on the same UI exposed an oversized interpretation heading;
the diary moved long model text into body paragraphs before the final freeze.

The tab is left as a user-facing preview. Web/worker PIDs and source path are in
live-processes.json. Runtime files remain outside application worktrees. The existing
port 8000 harness was left intact; this preview uses port 8001.
