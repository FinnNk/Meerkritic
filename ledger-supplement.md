The initial export retained the event payload and helper receipt in records,
but omitted the directory-based canonical event. This supplementary archive commit
adds its exact bytes without changing the application, bundle or manifest.
The previous archive commit remains preserved.
