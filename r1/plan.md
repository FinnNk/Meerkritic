# Frozen-result proposition plan

Base: 0bf957bcb0ae47e2c525f5f652830c06d5b8b9c9.
Diary candidate: 45c29fcfea43c99026a9ae54051df61d905e30fc.
Reconstruction requires the isolated diary verification to pass for this identity.
One integrator; material runtime/persistence contracts and workflow policy. No
new scope emerged beyond authorised review findings and their direct validation.

## Ordered series

| ID | Complete proposition / prerequisites | Concrete challenge and evidence | Diary mapping |
| --- | --- | --- | --- |
| P1 | Milestone-wide reviews and build/checkpoint/aggregate obligations; base | Existing generic principles missed caller contracts: explicit assessment areas, adverse challenges and recording template agree with AGENTS and slice template. No new lint quota. | ef5b8b2 |
| P2 | Honest completion/outcome states; base | Empty error, missing/non-hex digest, SQL blank error and contradictory outcome cannot produce success or extra events. Schema, store, workflow and regression tests together. | 1d01b07, e54439b |
| P3 | Explicit artefact metadata, catalogue and maintenance; P2 completion references | Body-key deception and non-object payload cannot choose/violate the store contract; legacy non-canonical bytes retain hash, metadata conflict and orphan behaviour retained. All callers and tests migrate together. | 05e8d4a, 9d881a9 |
| P4 | Source/annotation dependency ownership and exclusive worker lifetime; P3 ports | Disable application pagination; pass a reader with inspect only; deny process lock and verify no recovery/claim. Retain real process death and failure tests. | 287967d |
| P5 | Provider-owned model execution controls; P4 worker for live path | Task prompt has no Qwen instruction; actual versioned template request does; non-Qwen route has neither. Real fixture/MAF compatibility and failed-provider tests. | e84fc76 |
| P6 | Static public contracts visible in architecture evidence; base | Change a signature/default/field without changing imports; delta must expose it. Schema mismatch rejected, same-generator capture documented. Correct AST whitespace expectation belongs here. | 24844da, 3cb5891 |
| P7 | Existing-operation caller contract backfill; P3/P4 interfaces | Read actual consumers/limits/errors; annotations/progress/history, queue inspection, composition and web capability conditions now explicit. No inference behaviour changed. | 394af60 |
| P8 | Reconcile maintained knowledge with integrated VS1; base/P1-P7 | Cross-check delivered runtime, glossary source semantics, ACTIVE status, tests overview and merged ADR claims; imported sources untouched. | 1780f77 |
| P9 | Milestone assessment and ADR confirmation; P1-P8 | All guidance areas and original findings have source/evidence/disposition, exact integrated baseline and explicit self-review/platform/model limits. ADR status and index match confirmation. | 45c29fc |

## Boundary choice

Alternative A: one aggregate refactor/docs commit. This obscures independently
assessable state, artefact, execution and model-control contracts and repeats the
review problem the owner identified. Alternative B (selected): the nine complete
propositions above in one PR, with corrections folded into their owning units.
Do not split them into models/adapters/tests/doc layer commits or nine small PRs.
The artefact caller migration and worker lifetime each need their consumers/tests
at the same checkpoint; splitting those would defer correctness. P7 is separate
because the owner explicitly requires existing-code backfills to be separate.
P9 necessarily aggregates confirmation and cannot substitute for checkpoint proof.

Every checkpoint gets its own clean Windows/Python 3.12 checkout, lock, installed
package identity and canonical checks. If any checkpoint needs later content,
return to diary or regroup before publication. Exact final tracked trees, modes
and ancestry must match; there is no authorisation to improve semantic content.

## Retained development discoveries

Early sandbox SQLite tests failed before assertions due to temporary database
access. Approved external temporary storage and host execution restored the
intended context; later full-suite evidence is separate. The architecture test
initially expected spaces around a default whereas ast.unparse emits limit: int=20;
3cb5891 corrects the assertion to the documented normalised representation, not
the contract. The failing first attempt is retained in the diary and this record.
A full check found mixed working-file line endings after patching; Ruff restored
consistent endings. No tracked semantic change resulted after Git normalisation;
final-development-checks.log retains the failure and checks-2 retains the rerun.

The legacy indexing challenge exposed that reserialising parsed JSON could change
its hash. The implementation catalogues verified original bytes instead. No tests
were removed or obligations retired. Existing tests adapt only to explicit new
interfaces; new challenges extend continuing coverage. Deprecation warnings from
pinned Starlette and yoyo are retained; no ignores or dependency upgrades added.
